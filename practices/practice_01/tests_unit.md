# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| Корректная сборка промпта и возврат результата | `ReviewService.review` с mock LLM | `diff = "some changes"`, LLM mock возвращает `"OK"` | Возврат `{ "comment": "OK" }`; промпт содержит исходный `diff` | TRAINING_PR.diff: формирование промпта и возврат `{"comment": answer}` в ReviewService |
| Исключение от LLM пробрасывается вверх | Поведение `ReviewService.review` при исключении LLM | LLM mock поднимает исключение | Исключение пробрасывается для обработки на уровне API (контролируемый 5xx в интеграции) | ADR/TO BE: обработка ошибок на уровне API, без проглатывания в сервисе |
| Валидация наличия поля `diff` | Функция валидации входа (единица валидации) | JSON без `diff` | Возврат структурированной ошибки/исключения валидации | TO BE/ADR: валидация до вызова ReviewService; контролируемый 4xx |
| Валидация типа `diff` | Функция валидации входа | JSON с `diff` некорректного типа | Возврат ошибки/исключения валидации | TO BE/ADR: валидация до вызова ReviewService |
| Ограничение размера `diff` | Функция проверки размера | `diff` длиной больше порога (порог — Неизвестно / требует уточнения) | Возврат ошибки/исключения «слишком большой вход» | TO BE/ADR: ограничение размера до вызова LLM |
| Корректный `diff` проходит валидацию | Композитная валидация (наличие/тип/размер) | Валидный `diff` разумного размера | Успешное прохождение валидации (без ошибок) | TO BE/ADR: успешный путь не меняется |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-05
- Что проверили и исправили сами: Разделили unit-уровень (валидация и сервис) от интеграции; не придумывали числовые пороги; использовали mock LLM для изоляции логики.
